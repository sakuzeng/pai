# 手工清单：这次没能观测的部分（2026-08-11）

[说明.md](说明.md) 里自动化跑到的都有原始数据。这一页列的是跑不到的，
按 roadmap 规矩如实声明——没测就是没测，不许拿「按源码推」冒充观测。

跑不到的原因有两个，都在环境不在设计：
① 合成鼠标事件需要「辅助功能」授权（osascript 未获授权，本机也没有 cliclick / pyobjc）；
② Terminal.app 的 AppleScript 自动化权限未授（AppleEvent 超时），抓不了它的屏。

---

## 一、鼠标（整块没测，且它是 feature 13 三个需求里两个的地基）

以下每条都需要真人在窗口里动一下鼠标。现成的探针可以直接用：

```sh
python3 docs/dev/features/13-20260811-alt-screen/evidence/20260811-alt-screen反向对照/探针/probe_alt.py /tmp/probe.log --hold 8
```

跑起来后到 `[D]` 阶段（屏上出现 `MOUSE-ON`）时按提示操作，收到的原始字节会打在屏上。

- [ ] 单击：收到的是不是 `\x1b[<0;列;行M`（按下）+ `\x1b[<0;列;行m`（松开）？
      列/行是不是 1-based（SGR 1006 规定是）？
- [ ] 滚轮：向上/向下各是不是 `\x1b[<64;…M` / `\x1b[<65;…M`？一格滚轮发几条？
      （触控板惯性滚动会不会发出一大串——这直接决定 pai 要不要做滚动合并）
- [ ] 拖动：按住移动时是不是 `button|32`（`\x1b[<32;…M`）？
- [ ] 1002 vs 1003 的实际差别：只开 1002 时，不按键移动鼠标是否安静？
      （说明.md 第 3 条已证明三档互斥，但「1003 到底吵到什么程度」没测）
- [ ] 开了鼠标上报之后，终端自己的选中复制还能不能用？
      这条是取舍级别的：CC 为此专门给了 `CLAUDE_CODE_DISABLE_MOUSE`
      逃生口，说明它确实会坏。要按住 Option（iTerm2）或 Fn 才能选吗？
- [ ] 开了鼠标上报之后，terminal 自己的滚轮回看 scrollback 还能不能用？
      （alt 屏本来就没有 scrollback，但用户的肌肉记忆是滚轮）

## 二、Terminal.app 的屏幕内容（只拿到间接证据）

- [ ] 重发 `?1049h` 之后，`ALT-CONTENT-1..3` 是不是真的从屏上消失了？
      现在的结论是从「光标回到行1 列1」推出来的，不是看到的。
      iTerm2 那边是抓屏看到的，两者一致，但 Terminal.app 这条仍是推理。
- [ ] Terminal.app 里退出 alt 后主屏是否肉眼可见地完好（探针的 `MAIN-BEFORE-1..5`）。
- [ ] 在 Terminal.app 里跑一次真 pai（交付前那次只在 iTerm2 里跑了）。

## 三、resize

- [ ] 人手拖窗口边框时 `SIGWINCH` 来几次？本次用 AppleScript 一次性设 bounds
      只来 1 次，而 CC 与 pi 的注释都说一次用户操作常发 2 次以上（窗口沉降）。
      pai 现有的「同尺寸事件丢弃」就是为这个准备的，但没在真人拖动下验过。
- [x] ~~alt 屏里 resize 之后那片混着主屏内容的残骸是不是肉眼也这样？~~
      2026-08-11 交付前那次已裁决：是 AppleScript `contents` 的假象，
      `screencapture` 截真窗口证明屏幕是干净的（[说明.md 第 7 条](说明.md)）。

## 四、要到实现阶段才测得了的（现在记着，交付前那次反向对照必须覆盖）

- [ ] 中文 IME 候选框在 alt 屏里的位置（feature 12 就欠着一条同款，见 TODO）。
      交付前那次只确认了硬件光标停在输入行（截图可见），候选框本身要真人打字才看得到。
- [ ] Ctrl+C / Ctrl+Z / SIGCONT 之后还在不在 alt 屏，出来的时候屏幕干不干净。
- [ ] 程序崩溃时有没有把终端留在 alt 屏里（用户会看到一个空屏 + 打不出字）。
      这是 alt-screen 独有的失败模式：main-screen 下最坏也就是留个乱掉的 dock。
      正常退出与 Python 异常两条路已有测试钉住（`test_tui_alt_lifecycle.py`），
      `kill -9` 兜不住也测不到——已登记 TODO 为交付遗留。
- [ ] tmux 里跑（CC 为 tmux -CC 专门写了自动禁用逻辑，见 K cc-alt-screen 第五节）。

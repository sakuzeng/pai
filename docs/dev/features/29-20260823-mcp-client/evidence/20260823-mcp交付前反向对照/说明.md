# MCP 交付前反向对照（2026-08-23，真实 API 回合）

roadmap 固定项「交付前：跑一个完整的真实回合，哪怕花钱」。场景：
`pai_playground/mcp-smoke/`（gitignored，已 git init）——项目级
`.pai/settings.json` 配一个 stdio MCP server（`tests/fake_mcp_server.py`，
真子进程、真协议）+ 项目级 allow 规则 `mcp__fake__*`；信任标记预置
（`mark_project_mcp_trusted`——once 无人可问，未信任会被门禁跳过，这正是
28 式门禁的语义）。然后真 DeepSeek 跑
`pai "查一下暗号是什么，并把暗号原样告诉我。"`——不点名工具，看模型是否
按 description（「返回固定暗号。」）自主匹配调用。

## 结果（第一跑即成功）

会话落盘核验（`sessions/20260823-210012-ba7afce1.jsonl`）三项全真：

- 模型自主调了工具：`mcp__fake__echo_token({})`——任务没点名任何工具名；
- tool result 回填含暗号与完整结果映射：
  `FAKE-MCP-TOKEN-4711\n第二个文本块\n[image: image/png，内容已略去（v1 不支持非文本内容）]`
  ——text 块 `\n` join 与非文本占位符都按 spec 兑现在真实链路上；
- 最终回答原样给出暗号（「暗号是 `FAKE-MCP-TOKEN-4711`，原样奉上」）。

链路上同时兑现的还有：项目级配置 + 信任标记放行（once 模式）、allow 规则
放行（无权限询问、无拒绝）、stdio 子进程随 run 结束被 finally 关闭
（进程无残留，`ps` 无 fake_mcp_server）。

验收标准 1（配置→连接→桥接→模型调用→结果）与 7（真实回合）在真实链路上
成立。样本量 = 1，不外推——与 25 交付前反向对照同一措辞纪律。

## 成本

一次 pai once 回合，两次模型往返，DeepSeek 计价可忽略。

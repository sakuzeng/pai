__pycache__/
*.egg-info/
.env
sessions/
.pytest_cache/
build/
dist/

# 第三方文档快照：版权归原作者，用 refs/fetch_deepseek_docs.py 自行生成
refs/deepseek-api/
# agent 跑测试留下的产物
pai_playground/

# 含雇主工作区信息，不入库、本地保留（R2#1 裁决 2026-08-09，见 TODO 对应条目）
knowledge/anna/
docs/dev/reviews/2026-08-09-体系评审.md

# macOS Finder 的目录元数据。误入库过一次（历史上 root 那个是被跟踪的），
# 2026-08-11 用 `git rm --cached` 摘掉。写成 `.DS_Store` 而不是 `/.DS_Store`：
# gitignore 里不带斜杠的模式匹配**任意层级**，子目录里冒出来的也一并挡住。
.DS_Store
